<!-- <h1>Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <strong>
        <%#= link_to task.title, task_path(task) %>
      </strong> <%# refactored %>
      <span>(<%#= task.completed ? "completed" : "not completed yet" %>)</span>
      <div><%#= task.details %></div>
      <strong>
        <%#= link_to "Edit", edit_task_path(task) %>
      </strong> <%# refactored %>
      <%#= button_to "Delete",
        <%#task_path(task),%>
        method: :delete,
        form: { data: { turbo_confirm: "Delete this task?" } } %>
    </li>
  <% end %>
</ul> -->


<h1 class="mb-4 text-center text-warning fw-bold">My Tasks 🍁</h1>

<div class="list-group shadow-sm">
  <% @tasks.each do |task| %>
    <div class="list-group-item d-flex justify-content-between align-items-start bg-light mb-2 rounded-3">
      <div>
        <h5 class="mb-1">
          <%= link_to task.title, task_path(task), class: "text-decoration-none text-dark fw-semibold" %>
        </h5>
        <p class="mb-1 text-secondary"><%= task.details %></p>
        <small class="<%= task.completed ? 'text-success' : 'text-danger' %>">
          <%= task.completed ? "✓ Completed" : "⏳ Not completed yet" %>
        </small>
      </div>

      <div class="d-flex gap-2">
        <%= link_to edit_task_path(task), class: "btn btn-sm btn-outline-secondary" do %>
          <i class="fa-solid fa-pen"></i>
        <% end %>
        <%= link_to task_path(task),
                    data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },
                    class: "btn btn-sm btn-outline-danger" do %>
          <i class="fa-solid fa-trash"></i>
        <% end %>
      </div>
    </div>
  <% end %>
</div>

<div class="text-center mt-4">
  <%= link_to new_task_path, class: "btn btn-lg btn-warning text-dark shadow" do %>
    <i class="fa-solid fa-plus"></i> Add New Task
  <% end %>
</div>
